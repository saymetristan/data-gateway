# Postgres (local: Docker Compose; prod: Supabase data-ingest connection string)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/data_gateway

# OpenRouter (LLM + embeddings)
OPENROUTER_API_KEY=sk-or-...
EMBEDDING_MODEL=qwen/qwen3-embedding-8b
EMBEDDING_DIMENSIONS=1024
LLM_MODEL=nvidia/nemotron-3-ultra-550b-a55b:free

# Cifrado de credenciales de fuentes externas (32 bytes base64)
CREDENTIALS_ENCRYPTION_KEY=base64-32-bytes

# API
PORT=3000
